import { postSchema } from "../post";
import { homeSchema } from "../home";
import { gearPostImageSchema, gearPostSchema } from "../gear";

export const schemas = [postSchema, homeSchema, gearPostSchema, gearPostImageSchema];
